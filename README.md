After Cloning Create a Virtual Enviroment and then Activate the Virtual Enviroment and install the Following:
<br>
```pip install requests```
<br>
```pip install Flask```
<br>
```pip install gunicorn```
<br><br>
To run the Flask Application type:
<br>
```flask run```
